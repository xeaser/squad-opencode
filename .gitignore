# --- Go ---
/bin/
/dist/
*.exe
*.test
*.out
coverage.out
coverage.html
vendor/
# Binary at repo root only (do not ignore cmd/squad-oc/)
/squad-oc
/squad-oc.exe

# --- OpenCode host install (not Squad source) ---
# First `opencode` launch writes a plugin runtime under .opencode/
node_modules/
.opencode/node_modules/
.opencode/package.json
.opencode/package-lock.json
.opencode/bun.lock
.opencode/bun.lockb
.opencode/bunfig.toml

# --- Squad scratch ---
.squad/comms/*
!.squad/comms/.gitkeep

# Local CLI scratch
tmp-export.json
squad-export.json

# --- Secrets ---
.env
.env.*
!.env.example
*.pem
*.key

# --- OS / editors ---
.DS_Store
Thumbs.db
Desktop.ini
*~
*.swp
*.swo
.idea/
.vscode/
*.log

# Agent session scratch (plans, specs, SDD) — not domain docs/
.superpowers/

# Playwright / browser session scratch
# Named MCP screenshots belong in .playwright-mcp/ (see AGENTS.md).
# /dummy-*.png is a safety net if an agent still writes to the repo root.
.playwright-mcp/
/playwright-report/
/blob-report/
/test-results/
/playwright/.cache/
/playwright/.auth/
/dummy-*.png
*.webm


# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
*.gate.lock*
